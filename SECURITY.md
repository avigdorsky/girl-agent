# security

токены лежат в `data/<slug>/config.json` локально. `data/` в `.gitignore`.

закоммитил случайно — отзови у источника (botfather `/revoke`, my.telegram.org для userbot, дашборд провайдера для llm-ключей), вычисти git-историю через `git filter-repo` или bfg, force-push, выпусти новый.

уязвимость — issue с тегом `security` или в личку. poc после фикса.

userbot работает от твоего аккаунта через mtproto. могут забанить.
